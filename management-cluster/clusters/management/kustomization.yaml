apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # This line makes Flux include your Crossplane manifests (via Kustomize),
  # the same directory your CRD points at:
  - ../../infrastructure/crossplane

  # (Optional) If you also want the CRD object to be applied from Git,
  # add a relative file path to it (see step 3 below).
  # - ./crossplane-kustomization.yaml
